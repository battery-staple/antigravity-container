# Legacy sandbox state and runtime files (if any)
.state/
.host-exec/
docker-compose.override.yml

# Whitelist template is tracked, local overrides in ~/.antigravity-sandbox/
config/whitelist.yaml
config/whitelist.yml
config/whitelist.json
!config/whitelist.default.yaml

# Python virtualenvs and caches
.venv/
__pycache__/
*.pyc

# macOS system files
.DS_Store
